import {
  Notify
} from 'quasar'

const config = {
  plugins: {
    Notify
  },
  config: {
    notify: { /* look at QuasarConfOptions from the API card */ }
  }
};

export default config;